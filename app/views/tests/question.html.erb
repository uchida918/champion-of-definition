<h2 class="page-header">問題</h2>

<h3 class="well"><%= @definition.term %></h3>
<p>(<%= @user_definition.present? ? @user_definition.custom_body : @definition.body %>)</p>

<p>ここに回答を打ち込んでください。</p>

  <%= form_for(:test, url: tests_answer_path) do |f| %>
  <%= hidden_field_tag :datas, @datas %>
  <%= hidden_field_tag :type, params[:type] %>
  <div class="form-group">
    <%= f.text_field :answer, class: 'form-control input-lg'%>
  </div>
  <div class="form-group">
    <%= f.submit '回答する！', class: 'btn btn-primary btn-lg' %>
  </div>
  <% end %>
</div>

